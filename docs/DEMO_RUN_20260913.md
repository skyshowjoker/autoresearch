# 完整任务演示记录

演示日期：2026-09-13

## 运行目标

尝试优化趋势策略的快均线周期，比较跨年份开发集、双倍成本压力测试和 parent/candidate 差异。

## 任务

```text
task_id: 1da86fb8a8d841da8e2755e6e430d775
title: 演示：任务关联修复
mode: optimize
dataset: demo_daily_v1
budget: 2 trials / 300 seconds
status: completed
```

任务通过控制台 API 创建，之后使用 `--task-id 1da86fb8a8d841da8e2755e6e430d775` 启动 session。任务状态由 `draft` → `running` → `completed`。

## 执行命令

```bash
cd /Users/mac/PycharmProjects/autoresearch
uv run prepare.py --dataset demo_daily_v1
uv run autoquant console --host 127.0.0.1 --port 8765
uv run autoquant experiment \
  --session artifacts/demo-task-linked-20260913 \
  --iterations 2 \
  --task-id 1da86fb8a8d841da8e2755e6e430d775 \
  --brief examples/research_brief.json \
  --generator '/Users/mac/PycharmProjects/autoresearch/.venv/bin/python /Users/mac/PycharmProjects/autoresearch/examples/local_generator.py --candidate {candidate} --brief {brief}'
```

## 结果

| 节点 | 修改 | score | 决策 |
|---|---|---:|---|
| baseline | fast SMA = 20 | -0.25938297545750094 | baseline |
| candidate 1 | fast SMA = 10 | -0.4009749450760531 | discard |
| candidate 2 | fast SMA = 10 | -0.4009749450760531 | discard |

两个候选都完成 3 个 development folds × 2 个成本场景。候选分数低于 baseline，因此没有替换 champion。该结果只证明流程正确，不证明策略有 alpha。

## 已验证的产物

session：`artifacts/demo-task-linked-20260913`

候选目录含：

- `context.json`
- `brief.json`
- `agent_run.json`
- `hypothesis.json`
- `diff.json`
- `comparison`（写入 `decision.json`）
- fold 净值 Parquet
- fold 订单和交易 JSON

数据库：`.cache/laboratory.db`

该任务共 3 条实验 lineage：baseline 1 条、候选 discard 2 条；3 条记录均关联同一个 `task_id`，候选均指向 baseline `parent_experiment_id`。

## 控制台查看

浏览器访问：`http://127.0.0.1:8765/`

可调用：

```bash
curl http://127.0.0.1:8765/api/research/tasks
curl http://127.0.0.1:8765/api/experiments
curl http://127.0.0.1:8765/api/experiments/<experiment_id>
curl http://127.0.0.1:8765/api/experiments/<experiment_id>/equity
curl http://127.0.0.1:8765/api/experiments/<experiment_id>/artifacts
```

final 审批仍然需要成功的 `profile=final` 实验；本演示是 `dev`，所以不能审批或导出发布包。这是预期的安全门禁。

## 复现说明

当前示例使用本地 Backtrader ORCL CSV 和固定 demo snapshot，数据仅用于软件验证。真实市场研究应先发布经过授权、复权、停牌、公司行为和 point-in-time 校验的数据集，再运行相同流程。
